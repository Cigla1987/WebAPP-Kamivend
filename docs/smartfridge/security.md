# Security

Raw claim codes, pairing codes, device credentials, and PINs are never stored. Production must terminate HTTPS. Organization and machine identity are derived server-side. Secrets are configured through environment variables.
