# Edge authentication

Edge routes require `Authorization: Bearer`. The credential hash resolves the edge device, SmartFridge profile, machine, type, enabled state, and organization. Browser cookies and payload IDs are not authoritative.
