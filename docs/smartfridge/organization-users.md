# Organization service users

Service credentials are separate from Better Auth passwords. PINs use salted scrypt verifiers, never plaintext. Only matching organization members are synchronized to an authenticated edge device.
