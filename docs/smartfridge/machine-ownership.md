# Machine ownership

Machines begin unassigned. An owner claims by serial number and one-time hashed activation code; organization identity comes from verified active membership. `createdBy` is audit data only.
