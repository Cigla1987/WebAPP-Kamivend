# URL of the Vending web app API used for authentication and data sync
# This file is bundled into the packaged app via electron-builder.
# For production builds, set via CI environment variables before `pnpm dist`.
VENDING_API_URL=http://localhost:3000
